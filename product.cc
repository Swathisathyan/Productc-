#include <iostream>
using namespace std;

int main()
{
    float a,b; float p; 
    cout<<"enter two numbers whose product is to be found:" ;
    cin >> a;
    cin >> b;
   
    cout<<"The product of the number is :"<< (a*b);
    return 0;
}
